
<h2><%= @animal.name %></h2>
<div>
  <%= link_to 'DOWNLOAD PDF', animal_path(::Animal.find(params[:id]), format: :pdf) %>
</div>
<div>
  <strong>Name:</strong> <%= @animal.name %><br>
  <strong>Age:</strong> <%= @animal.age %><br>
  <strong>Species:</strong> <%= @animal.species %>
</div>

<%= link_to 'Home', animals_path %>